# @package runner
# Backend implemented in the deferred Ray issue (#188, excluded from this build pass).
family: ray
batch_size: 64
task_runner:
  _target_: prefect_ray.RayTaskRunner
  address: null # null -> Ray starts its own local cluster
